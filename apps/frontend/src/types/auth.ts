// Data types for Login Input fields
export type LoginUserFieldInputData = {
  userId: string;
  password: string;
};

// Data type for Change Password Input fields
export type ChangePasswordInputData = {
  newPassword: string;
  confirmPassword: string;
};
